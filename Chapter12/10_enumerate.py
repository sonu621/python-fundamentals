# ----------------- Enumerate() ------------------

# enumerate() is a built-in Python function that allows you to loop over an iterable (like a list, tuple, or string)
# and get both the index and the value at the same time.

fruits = ["apple", "banana", "cherry"]

for index, fruit in enumerate(fruits):
    print(index, fruit)


